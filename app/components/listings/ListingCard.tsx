'use client'

import {Listing} from "@prisma/client";

type Props = {
    data: Listing
};

const ListingCard = ({data}: Props) => {
    return (
        <div>

        </div>
    );
};

export default ListingCard;